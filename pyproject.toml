[project]
name = "crewai-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "crewai[google-genai,tools]>=1.15.9",
]

[dependency-groups]
dev = [
    "ruff>=0.16.0",
]
